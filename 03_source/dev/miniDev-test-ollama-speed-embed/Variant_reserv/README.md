# Ollama Embedding Models Performance Tester (Variant C)

Профессиональный инструмент для тестирования производительности embedding моделей в Ollama, работающем в Docker контейнере. Оптимизирован для слабого железа с расширенными таймаутами и надежной обработкой ошибок.

## 🚀 Особенности

- **Async/Sync гибридная архитектура** для оптимальной производительности
- **Мониторинг Docker контейнера** с автоматической проверкой статуса
- **Комплексный сбор метрик** (время, размерность векторов, токены/сек)
- **Множественные форматы вывода** (JSON, CSV, Markdown, HTML)
- **Механизм повторных попыток** с экспоненциальной задержкой
- **Память-эффективная пакетная обработка**
- **Real-time отслеживание прогресса** с Rich UI
- **Warmup запросы** для стабилизации результатов
- **Детальная статистика** (среднее, медиана, мин, макс, стд. отклонение)

## 📋 Требования

- Python 3.10+
- Docker Desktop с запущенным контейнером Ollama
- Установленные embedding модели в Ollama

## 🔧 Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Убедитесь, что Docker контейнер Ollama запущен:
```bash
docker ps | grep ollama
```

3. Проверьте доступные модели:
```bash
docker exec ollama ollama list
```

## 📊 Список тестируемых моделей

По умолчанию тестируются следующие модели:

- `embeddinggemma:300m-qat-q8_0` (338 MB)
- `qwen3-embedding:0.6b-fp16` (1.2 GB)
- `qwen3-embedding:0.6b-q8_0` (639 MB)
- `embeddinggemma:300m-qat-q4_0` (238 MB)
- `all-minilm:22m-l6-v2-fp16` (45 MB)
- `bge-m3:567m-fp16` (1.2 GB)
- `embeddinggemma:300m-bf16` (621 MB)
- `nomic-embed-text:137m-v1.5-fp16` (274 MB)
- `qllama/multilingual-e5-small:f16` (241 MB)
- `all-minilm:l6-v2` (45 MB)

## 🎯 Использование

### Базовый запуск

Тестирование всех моделей с настройками по умолчанию:
```bash
python test_ollama_embeddings.py
```

### Запуск с конфигурацией

Использование файла конфигурации:
```bash
python test_ollama_embeddings.py --config config.json
```

### Тестирование конкретных моделей

Тестирование только выбранных моделей:
```bash
python test_ollama_embeddings.py --models qwen3-embedding:0.6b-q8_0 all-minilm:l6-v2
```

### Изменение количества тестов

Запуск 10 тестов на каждую модель:
```bash
python test_ollama_embeddings.py --tests-per-model 10
```

### Изменение таймаута

Для очень слабого железа увеличьте таймаут:
```bash
python test_ollama_embeddings.py --timeout 300
```

### Проверка статуса Docker

Только проверка Docker без запуска тестов:
```bash
python test_ollama_embeddings.py --docker-check
```

### Изменение URL Ollama

Если Ollama работает на другом порту:
```bash
python test_ollama_embeddings.py --url http://localhost:11435
```

### Verbose режим

Включение детального логирования:
```bash
python test_ollama_embeddings.py -v
```

### Сохранение в другую директорию

```bash
python test_ollama_embeddings.py --output-dir ./results
```

## ⚙️ Конфигурация

Файл [`config.json`](config.json) содержит все настройки:

```json
{
  "ollama_url": "http://localhost:11434",
  "timeout": 180,
  "max_retries": 3,
  "tests_per_model": 5,
  "warmup_requests": 1,
  "interval_between_tests": 3,
  "models": null,
  "output_dir": ".",
  "check_docker": true,
  "save_json": true,
  "save_csv": true,
  "save_markdown": true,
  "save_html": true
}
```

### Параметры конфигурации

- **ollama_url**: URL Ollama API (по умолчанию: `http://localhost:11434`)
- **timeout**: Таймаут запросов в секундах (по умолчанию: 180)
- **max_retries**: Максимальное количество повторных попыток (по умолчанию: 3)
- **tests_per_model**: Количество тестов на модель (по умолчанию: 5)
- **warmup_requests**: Количество warmup запросов перед тестами (по умолчанию: 1)
- **interval_between_tests**: Пауза между тестами в секундах (по умолчанию: 3)
- **models**: Список моделей для тестирования (`null` = все модели)
- **output_dir**: Директория для сохранения результатов (по умолчанию: `.`)
- **check_docker**: Проверять статус Docker контейнера (по умолчанию: `true`)
- **save_json**: Сохранять результаты в JSON (по умолчанию: `true`)
- **save_csv**: Сохранять результаты в CSV (по умолчанию: `true`)
- **save_markdown**: Сохранять результаты в Markdown (по умолчанию: `true`)
- **save_html**: Сохранять результаты в HTML (по умолчанию: `true`)

## 📈 Форматы вывода

Скрипт генерирует результаты в 4 форматах:

### 1. JSON (`ollama_embedding_test_YYYYMMDD_HHMMSS.json`)
Полный отчет со всеми метриками и детальными результатами каждого теста.

### 2. CSV (`ollama_embedding_test_YYYYMMDD_HHMMSS.csv`)
Табличный формат для анализа в Excel/Google Sheets:
- Model
- Status
- Successful Tests
- Avg Duration (s)
- Min Duration (s)
- Max Duration (s)
- Median Duration (s)
- Std Dev (s)
- Embedding Dim
- Tokens/sec

### 3. Markdown (`ollama_embedding_test_YYYYMMDD_HHMMSS.md`)
Читаемый отчет с рейтингами и детальными результатами.

### 4. HTML (`ollama_embedding_test_YYYYMMDD_HHMMSS.html`)
Интерактивный HTML отчет с таблицами и стилизацией.

## 🔍 Метрики

Для каждой модели собираются следующие метрики:

- **Статус**: SUCCESS, FAILED, ERROR, TIMEOUT
- **Успешные тесты**: Количество успешных запросов
- **Среднее время**: Среднее время выполнения запроса
- **Минимальное время**: Самый быстрый запрос
- **Максимальное время**: Самый медленный запрос
- **Медиана времени**: Медианное значение времени
- **Стандартное отклонение**: Вариативность времени выполнения
- **Размерность embedding**: Размер выходного вектора
- **Токены/сек**: Скорость обработки (приблизительная)

## 🐳 Docker команды

### Проверка статуса контейнера
```bash
docker ps | grep ollama
```

### Список моделей в Ollama
```bash
docker exec ollama ollama list
```

### Запуск контейнера Ollama (если не запущен)
```bash
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
```

### Загрузка модели
```bash
docker exec ollama ollama pull qwen3-embedding:0.6b-q8_0
```

### Просмотр логов контейнера
```bash
docker logs ollama
```

## 🛠️ Устранение неполадок

### Ollama недоступен
```
❌ Error: Ollama is not available at http://localhost:11434
```
**Решение**: Убедитесь, что Docker контейнер запущен:
```bash
docker start ollama
```

### Таймаут запросов
```
Timeout after 180s
```
**Решение**: Увеличьте таймаут для слабого железа:
```bash
python test_ollama_embeddings.py --timeout 300
```

### Модель не найдена
```
Error: Model not found
```
**Решение**: Загрузите модель в Ollama:
```bash
docker exec ollama ollama pull <model-name>
```

### Docker не установлен
```
❌ Error: Docker is not installed
```
**Решение**: Установите Docker Desktop с официального сайта.

## 📝 Примеры использования

### Быстрое тестирование топ-3 моделей
```bash
python test_ollama_embeddings.py \
  --models all-minilm:l6-v2 qwen3-embedding:0.6b-q8_0 embeddinggemma:300m-qat-q4_0 \
  --tests-per-model 3
```

### Полное тестирование с максимальной точностью
```bash
python test_ollama_embeddings.py \
  --tests-per-model 10 \
  --timeout 300 \
  --output-dir ./detailed_results \
  -v
```

### Тестирование без проверки Docker
```bash
python test_ollama_embeddings.py --no-docker-check
```

## 🏗️ Архитектура

Скрипт использует современную архитектуру Python:

- **Async/Await**: Асинхронные HTTP запросы через `httpx`
- **Type Hints**: Полная типизация для безопасности кода
- **Dataclasses**: Структурированные данные с валидацией
- **Rich UI**: Красивый терминальный интерфейс
- **Pydantic**: Валидация конфигурации
- **Retry Logic**: Автоматические повторные попытки с экспоненциальной задержкой

## 📊 Пример вывода

```
🤖 Ollama Embedding Models Test Results

┌─────────────────┬────────┐
│ Metric          │ Value  │
├─────────────────┼────────┤
│ Total Models    │ 10     │
│ Successful      │ 9      │
│ Failed          │ 1      │
│ Test Duration   │ 245.32s│
└─────────────────┴────────┘

🏆 Top 5 Fastest Models

┌──────┬────────────────────────────────┬──────────────┬───────────────┐
│ Rank │ Model                          │ Avg Time (s) │ Embedding Dim │
├──────┼────────────────────────────────┼──────────────┼───────────────┤
│ 1    │ all-minilm:l6-v2               │ 0.1234       │ 384           │
│ 2    │ embeddinggemma:300m-qat-q4_0   │ 0.2456       │ 256           │
│ 3    │ qwen3-embedding:0.6b-q8_0      │ 0.3789       │ 768           │
└──────┴────────────────────────────────┴──────────────┴───────────────┘

📁 Results saved:
  • JSON: ollama_embedding_test_20241224_170000.json
  • CSV: ollama_embedding_test_20241224_170000.csv
  • Markdown: ollama_embedding_test_20241224_170000.md
  • HTML: ollama_embedding_test_20241224_170000.html

✅ Testing complete! 9/10 models tested successfully.
```

## 🤝 Вклад

Этот скрипт является частью проекта тестирования Ollama моделей. Для улучшений и предложений создавайте issue или pull request.

## 📄 Лицензия

MIT License

## 🔗 Полезные ссылки

- [Ollama Documentation](https://github.com/ollama/ollama)
- [Docker Documentation](https://docs.docker.com/)
- [Embedding Models Guide](https://ollama.com/blog/embedding-models)

---

**Примечание**: Скрипт оптимизирован для слабого железа с увеличенными таймаутами и паузами между запросами. Для мощных систем можно уменьшить `timeout` и `interval_between_tests` в конфигурации.