# Запрос для запуска тестирования

cd "miniDev-test-ollama-speed-embed/Variant_base" && venv\Scripts\activate.bat && python test_ollama_speed_pro.py --config config.yaml

Если не работает первый запрос, то попробовать резервный запрос:
cd "miniDev-test-ollama-speed-embed/Variant_reserv" && run.bat