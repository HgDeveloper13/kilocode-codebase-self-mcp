# Ollama Embedding Models Speed Tester (Variant B)

Professional Python инструмент для тестирования производительности embedding моделей в Ollama, работающей в Docker контейнере.

## Особенности

- 📊 **Комплексное тестирование** - измерение скорости, размерности embedding и throughput
- 🔄 **Retry logic** - автоматические повторы при временных сбоях
- ⏱️ **Увеличенные таймауты** - оптимизировано для слабого железа
- 📁 **Multiple output formats** - JSON, CSV и читаемый текстовый отчет
- 🎨 **Красивый вывод** - использует библиотеку Rich для терминала
- ⚙️ **Гибкая конфигурация** - YAML конфигурация + CLI аргументы

## Требования

- Python 3.10+
- Ollama запущенная в Docker Desktop
- Интернет для установки зависимостей

## Установка

```bash
# Клонируйте репозиторий и перейдите в директорию
cd miniDev-test-ollama-speed-embed/variant test B

# Установите зависимости
pip install -r requirements.txt
```

## Использование

### Базовый запуск

```bash
# Запуск с настройками по умолчанию
python test_ollama_speed_pro.py

# Запуск с конфигурацией из файла
python test_ollama_speed_pro.py --config config.yaml

# Тестирование конкретных моделей
python test_ollama_speed_pro.py --models qwen3-embedding:0.6b-q8_0 all-minilm:l6-v2

# Увеличенное количество тестов
python test_ollama_speed_pro.py --tests-per-model 10 --verbose
```

### Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `-c, --config` | Путь к YAML конфигурации | None |
| `-u, --url` | URL Ollama API | http://localhost:11434 |
| `-m, --models` | Список моделей для тестирования | Все модели |
| `-t, --tests-per-model` | Количество тестов на модель | 5 |
| `--timeout` | Таймаут запроса в секундах | 120 |
| `-o, --output-dir` | Директория для результатов | Текущая |
| `-v, --verbose` | Подробное логирование | False |

### Конфигурационный файл (config.yaml)

```yaml
ollama_url: "http://localhost:11434"
timeout: 120
max_retries: 3
retry_delay: 5
tests_per_model: 5
interval_between_tests: 2
models: null  # null = все модели
output_dir: "."
```

## Модели для тестирования

Скрипт тестирует следующие embedding модели:

1. `embeddinggemma:300m-qat-q8_0` - 338 MB
2. `qwen3-embedding:0.6b-fp16` - 1.2 GB
3. `qwen3-embedding:0.6b-q8_0` - 639 MB
4. `embeddinggemma:300m-qat-q4_0` - 238 MB
5. `all-minilm:22m-l6-v2-fp16` - 45 MB
6. `bge-m3:567m-fp16` - 1.2 GB
7. `embeddinggemma:300m-bf16` - 621 MB
8. `nomic-embed-text:137m-v1.5-fp16` - 274 MB
9. `qllama/multilingual-e5-small:f16` - 241 MB
10. `all-minilm:l6-v2` - 45 MB

## Пример вывода

```
🤖 Ollama Embedding Models Test Results

┌─────────────────┬────────┐
│ Metric          │ Value  │
├─────────────────┼────────┤
│ Total Models    │ 10     │
│ Successful      │ 8      │
│ Failed          │ 2      │
└─────────────────┴────────┘

🏆 Top 5 by Speed

┌──────┬───────────────────────────────────┬──────────┬───────┬───────────┐
│ Rank │ Model                             │ Time (s) │ Dim   │ Tokens/s  │
├──────┼───────────────────────────────────┼──────────┼───────┼───────────┤
│ 1    │ all-minilm:l6-v2                  │ 0.0152   │ 384   │ 25263.15  │
│ 2    │ all-minilm:22m-l6-v2-fp16         │ 0.0187   │ 384   │ 20534.76  │
│ 3    │ embeddinggemma:300m-qat-q4_0      │ 0.0423   │ 384   │ 9077.54   │
│ 4    │ qwen3-embedding:0.6b-q8_0         │ 0.0654   │ 768   │ 11743.21  │
│ 5    │ nomic-embed-text:137m-v1.5-fp16   │ 0.0891   │ 768   │ 8621.45   │
└──────┴───────────────────────────────────┴──────────┴───────┴───────────┘

📁 Results saved:
  • JSON: ollama_embedding_test_20241224_170000.json
  • CSV: ollama_embedding_test_20241224_170000.csv
  • TXT: ollama_embedding_test_20241224_170000_report.txt

✅ Testing complete! 8/10 models tested.
```

## Файлы результатов

После запуска создаются следующие файлы:

- `{timestamp}_ollama_embedding_test.json` - полные данные в JSON
- `{timestamp}_ollama_embedding_test.csv` - таблица результатов
- `{timestamp}_ollama_embedding_test_report.txt` - читаемый отчет

## Устранение проблем

### Ollama недоступна

Убедитесь что Docker контейнер с Ollama запущен:

```bash
# Проверка статуса контейнера
docker ps | grep ollama

# Просмотр списка моделей
docker exec ollama ollama list
```

### Таймауты

Если возникают таймауты, увеличьте параметр `timeout` в конфигурации:

```bash
python test_ollama_speed_pro.py --timeout 180
```

### Ошибки подключения

Проверьте что Ollama API доступен по URL:

```bash
curl http://localhost:11434/api/tags
```

## Разработка

### Установка dev зависимостей

```bash
pip install -e ".[dev]"
```

### Запуск тестов

```bash
pytest tests/
```

### Форматирование кода

```bash
black test_ollama_speed_pro.py
mypy test_ollama_speed_pro.py
```

## Лицензия

MIT