# 🔧 Python скрипты для Qdrant Manager

Система управления Qdrant коллекциями с гибкой системой конфигурации.

## 📁 Структура файлов

```
python/
├── config.yaml                    # 📋 Конфигурационный файл (новое!)
├── qdrant_config.py              # ⚙️ Система управления конфигурацией (новое!)
├── qdrant_manager_with_config.py # 🆕 Новый менеджер с поддержкой конфигурации
├── qdrant_manager.py             # 🔄 Старый менеджер (обратная совместимость)
├── check_status.py               # 🔍 Скрипт проверки статуса
├── check_and_fix.py              # 🔧 Скрипт исправления коллекций
├── test_config_system.py         # 🧪 Комплексное тестирование (новое!)
├── README_qdrant.md              # 📖 Полная документация
└── USAGE.md                      # 📋 Инструкции по использованию (это файл)
```

## 🚀 Быстрый старт

### Способ 1: С использованием конфигурации (рекомендуется)

1. **Создайте конфигурационный файл** `config.yaml`:

```yaml
# config.yaml
qdrant:
  host: "localhost"
  port: 6333
  api_key: "your-api-key-here"
  timeout: 30
  collections:
    default_threshold: 1
    batch_size: 100
```

2. **Запустите скрипты**:

```bash
cd python
python check_status.py
python check_and_fix.py
```

### Способ 2: Без конфигурации (обратная совместимость)

```bash
cd python
python check_status.py
python check_and_fix.py
```

> **Примечание:** Старые скрипты продолжают работать как раньше, используя константы в коде.

## ⚙️ Настройка конфигурации

### 📝 Создание конфигурационного файла

#### YAML формат (рекомендуется)

```yaml
# config.yaml
qdrant:
  host: "localhost"
  port: 6333
  api_key: "your-api-key-here"
  timeout: 30
  collections:
    default_threshold: 1
    batch_size: 100
```

#### JSON формат

```json
{
  "qdrant": {
    "host": "localhost",
    "port": 6333,
    "api_key": "your-api-key-here",
    "timeout": 30,
    "collections": {
      "default_threshold": 1,
      "batch_size": 100
    }
  }
}
```

### 🔧 Переменные окружения

Переменные окружения имеют **высший приоритет** над файлом конфигурации:

```bash
# Linux/macOS
export QDRANT_HOST="your-host.com"
export QDRANT_PORT="6333"
export QDRANT_API_KEY="your-api-key"
export QDRANT_TIMEOUT="60"

# Windows (cmd)
set QDRANT_HOST=your-host.com
set QDRANT_PORT=6333
set QDRANT_API_KEY=your-api-key
set QDRANT_TIMEOUT=60

# Windows (PowerShell)
$env:QDRANT_HOST="your-host.com"
$env:QDRANT_PORT="6333"
$env:QDRANT_API_KEY="your-api-key"
$env:QDRANT_TIMEOUT="60"
```

### 🔒 Безопасное хранение API ключей

**Рекомендуемые способы:**

1. **Переменные окружения** (рекомендуется):
```bash
export QDRANT_API_KEY="your-secret-api-key"
```

2. **Отдельный файл конфигурации**:
```yaml
# config.yaml
qdrant:
  host: "localhost"
  port: 6333
  api_key: "your-api-key-here"
  timeout: 30
```

3. **Секреты системы** (для production):
```bash
# Использование системных секретов
export QDRANT_API_KEY=$(cat /run/secrets/qdrant_api_key)
```

## 📊 Описание скриптов

### 🔍 check_status.py - Проверка статуса коллекций

**Что делает:**
- ✅ Анализирует все коллекции в Qdrant
- 📊 Показывает количество точек и статус индексации
- ⚠️ Выявляет коллекции с высоким `indexing_threshold`
- 💾 Сохраняет отчет в файл `qdrant_status_YYYYMMDD_HHMMSS.txt`

**Запуск:**
```bash
python check_status.py
```

**Пример вывода:**
```
============================================================
🔍 QDRANT COLLECTIONS STATUS CHECKER
============================================================
🕐 Время проверки: 2025-11-10 16:04:53
🌐 URL: https://your-cluster.qdrant.io:6333

🔄 Получаю статус всех коллекций...

============================================================
🔍 СТАТУС КОЛЛЕКЦИЙ
============================================================
  1/5 ✅ collection_1         | 1000 точек |  950 (95.0%) | ОПТИМИЗИРОВАНА
  2/5 ⚠️  collection_2         |  500 точек |  400 (80.0%) | НУЖНО ИСПРАВИТЬ
  3/5 ❌ collection_3         | ОШИБКА: Connection timeout
  4/5 ✅ collection_4         |  200 точек |  200 (100.0%) | ОПТИМИЗИРОВАНА
  5/5 ⚠️  collection_5         |  800 точек |  600 (75.0%) | НУЖНО ИСПРАВИТЬ

============================================================
📊 СВОДКА
============================================================
📦 Всего коллекций: 5
✅ Успешно: 4
🚨 С ошибками: 1
📈 Всего точек: 2,500
🔍 Индексировано: 2,150 (86.0%)

🎯 Статус оптимизации:
   ✅ Оптимизированы: 2
   ⚠️  Нужно исправить: 2

💡 Рекомендация: Запустите python/check_and_fix.py для автоматического исправления
```

### 🔧 check_and_fix.py - Исправление коллекций

**Что делает:**
- 🔍 Анализирует текущее состояние
- ⚠️ Показывает коллекции для исправления
- ❓ Запрашивает подтверждение пользователя
- 🔧 Автоматически исправляет `indexing_threshold = 1`
- ✅ Проверяет результаты исправления
- 💾 Сохраняет подробный отчет

**Запуск:**
```bash
python check_and_fix.py
```

**Пример вывода:**
```
============================================================
🔧 QDRANT COLLECTIONS FIXER
============================================================
🕐 Время запуска: 2025-11-10 16:04:53
🌐 URL: https://your-cluster.qdrant.io:6333

🔍 Анализирую текущее состояние коллекций...

============================================================
🔍 АНАЛИЗ СОСТОЯНИЯ
============================================================
📦 Всего коллекций: 5
⚠️  Нужно исправить: 2
✅ Уже оптимизированы: 2
🚨 С ошибками: 1

📋 Коллекции для исправления:
  1. collection_2
  2. collection_5

❓ Найдено 2 коллекций для исправления. Продолжить? [y/N]: y

============================================================
🔧 ЗАПУСК ИСПРАВЛЕНИЯ
============================================================
🔄 Исправляю 2 коллекций...
   Устанавливаю indexing_threshold = 1
   Устанавливаю vacuum_min_vector_number = 100

🔄 Найдено коллекций: 5
🔧 Обрабатываю коллекцию: collection_2
✅ collection_2 - исправлена
🔧 Обрабатываю коллекцию: collection_5
✅ collection_5 - исправлена

📊 РЕЗУЛЬТАТЫ ИСПРАВЛЕНИЯ:
✅ Успешно: 2/2
❌ Ошибки: 0/2

✅ Успешно исправлены:
  • collection_2
  • collection_5

============================================================
🔍 ПРОВЕРКА РЕЗУЛЬТАТОВ
============================================================
🔍 Проверяю новые значения indexing_threshold...

✅ Проверка завершена:
   • Подтверждено исправление: 2
   • Все еще нужно исправить: 0
   • Ошибки при проверке: 0

💾 Подробный отчет сохранен в: qdrant_fix_report_20251110_160453.txt
```

## 🧪 Тестирование системы

### Запуск комплексного тестирования

```bash
# Установка зависимостей для тестирования
pip install -r test_requirements.txt

# Запуск всех тестов
python test_config_system.py

# Или с использованием pytest
pytest test_config_system.py -v
```

### Тесты покрывают:

- ✅ Базовую функциональность загрузки конфигурации
- ✅ Приоритет переменных окружения над файлом
- ✅ Валидацию параметров конфигурации
- ✅ Интеграцию всех обновленных скриптов
- ✅ Обратную совместимость со старыми импортами
- ✅ Обработку ошибок и исключений

## 🔄 Полный цикл работы

### С конфигурацией

```bash
# 1. Создаем конфигурационный файл
cat > config.yaml << EOF
qdrant:
  host: "localhost"
  port: 6333
  api_key: "your-api-key-here"
  timeout: 30
EOF

# 2. Проверяем статус
python check_status.py

# 3. Если есть коллекции для исправления - запускаем исправление
python check_and_fix.py

# 4. Снова проверяем статус для подтверждения
python check_status.py
```

### Без конфигурации (обратная совместимость)

```bash
# 1. Сначала проверяем статус
python check_status.py

# 2. Если есть коллекции для исправления - запускаем исправление
python check_and_fix.py

# 3. Снова проверяем статус для подтверждения
python check_status.py
```

## 📋 Требования

- Python 3.6+
- Модуль `requests` (устанавливается: `pip install requests`)
- Модуль `PyYAML` (для YAML конфигурации: `pip install PyYAML`)
- Доступ к Qdrant API с валидным API ключом

## 📊 Отчеты

### Отчет о статусе (`qdrant_status_YYYYMMDD_HHMMSS.txt`)
Содержит:
- Время проверки и URL
- Сводку по всем коллекциям
- Детали по каждой коллекции
- Рекомендации

### Отчет об исправлении (`qdrant_fix_report_YYYYMMDD_HHMMSS.txt`)
Содержит:
- Начальное состояние
- Результаты исправления
- Проверку результатов
- Итоговое состояние

## 🎯 Преимущества новой системы

1. **Гибкая настройка**: Конфигурация через YAML/JSON файлы
2. **Переменные окружения**: Приоритет env vars над файлами
3. **Валидация**: Автоматическая проверка параметров
4. **Безопасность**: API ключи в отдельных файлах
5. **Обратная совместимость**: Старые скрипты продолжают работать

## 📞 Поддержка

- [README_qdrant.md](README_qdrant.md) - Полная документация
- [test_config_system.py](test_config_system.py) - Комплексное тестирование
- [qdrant_config_analysis.md](qdrant_config_analysis.md) - Анализ системы