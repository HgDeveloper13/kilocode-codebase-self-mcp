# 🔄 Руководство по миграции на систему конфигурации

Это руководство поможет вам перейти с старой системы на новую систему конфигурации.

## 📋 Содержание

- [Что изменилось](#что-изменилось)
- [Преимущества новой системы](#преимущества-новой-системы)
- [Пошаговая миграция](#пошаговая-миграция)
- [Примеры кода](#примеры-кода)
- [Часто задаваемые вопросы](#часто-задаваемые-вопросы)
- [Обратная совместимость](#обратная-совместимость)

## Что изменилось

### Старая система
- Конфигурация жестко закодирована в Python файлах
- API ключи хранятся в коде
- Нет гибкой настройки для разных окружений
- Сложно масштабировать и поддерживать

### Новая система
- Гибкая конфигурация через YAML/JSON файлы
- Поддержка переменных окружения
- Автоматическая валидация параметров
- Безопасное хранение API ключей
- Обратная совместимость

## Преимущества новой системы

✅ **Безопасность**: API ключи не хранятся в коде  
✅ **Гибкость**: Легкая смена конфигурации без изменения кода  
✅ **Окружения**: Разные настройки для dev/staging/prod  
✅ **Валидация**: Автоматическая проверка параметров  
✅ **Обратная совместимость**: Старый код продолжает работать  

## Пошаговая миграция

### Шаг 1: Создание конфигурационного файла

**Создайте файл `config.yaml` в папке `python/`:**

```yaml
# config.yaml
qdrant:
  host: "localhost"
  port: 6333
  api_key: "your-api-key-here"
  timeout: 30
  collections:
    default_threshold: 1
    batch_size: 100
```

### Шаг 2: Обновление импортов (опционально)

**Старый способ (продолжает работать):**
```python
from qdrant_manager import QdrantConfigManager

QDRANT_URL = "https://your-cluster.qdrant.io:6333"
API_KEY = "your_api_key_here"

manager = QdrantConfigManager(QDRANT_URL, API_KEY)
```

**Новый способ (рекомендуется):**
```python
from qdrant_manager_with_config import QdrantManager

# Автоматическая загрузка из config.yaml
manager = QdrantManager()
```

### Шаг 3: Настройка переменных окружения (рекомендуется)

```bash
# Linux/macOS
export QDRANT_HOST="your-host.com"
export QDRANT_PORT="6333"
export QDRANT_API_KEY="your-api-key"
export QDRANT_TIMEOUT="60"

# Windows
set QDRANT_HOST=your-host.com
set QDRANT_PORT=6333
set QDRANT_API_KEY=your-api-key
set QDRANT_TIMEOUT=60
```

### Шаг 4: Тестирование

```bash
# Запуск тестов
python test_config_system.py

# Проверка работы
python check_status.py
```

## Примеры кода

### Пример 1: Простая миграция

**Было:**
```python
from qdrant_manager import QdrantConfigManager

QDRANT_URL = "https://your-cluster.qdrant.io:6333"
API_KEY = "your_api_key_here"

manager = QdrantConfigManager(QDRANT_URL, API_KEY)
results = manager.fix_all_collections()
```

**Стало:**
```python
from qdrant_manager_with_config import QdrantManager

# Создаем config.yaml с настройками
manager = QdrantManager()  # Автоматически загружает конфиг
results = manager.fix_all_collections()
```

### Пример 2: Использование с переменными окружения

```python
from qdrant_manager_with_config import QdrantManager
import os

# Установка переменных окружения
os.environ['QDRANT_HOST'] = 'production-host.com'
os.environ['QDRANT_API_KEY'] = 'production-key'

# Автоматическое использование env vars
manager = QdrantManager()
results = manager.fix_all_collections()
```

### Пример 3: Кастомная конфигурация

```python
from qdrant_manager_with_config import QdrantManager

# Загрузка из кастомного файла
manager = QdrantManager(config_path="production_config.yaml")

# Использование с кастомными параметрами
manager = QdrantManager(
    config_path="config.yaml",
    host="custom-host.com",
    port=8080
)
```

## Часто задаваемые вопросы

### ❓ Нужно ли переписывать весь код?

**Нет!** Старый код продолжает работать без изменений. Вы можете мигрировать постепенно.

### ❓ Где хранить API ключи?

**Рекомендуемые способы:**
1. Переменные окружения (рекомендуется)
2. Отдельный файл конфигурации
3. Системные секреты (для production)

### ❓ Что делать с существующими скриптами?

**Варианты:**
1. Оставить как есть (обратная совместимость)
2. Постепенно переносить на новую систему
3. Использовать обе системы параллельно

### ❓ Как переключаться между окружениями?

**Просто измените переменные окружения:**

```bash
# Для development
export QDRANT_HOST="dev-host.com"
export QDRANT_API_KEY="dev-key"

# Для production
export QDRANT_HOST="prod-host.com"
export QDRANT_API_KEY="prod-key"
```

### ❓ Что делать при ошибках конфигурации?

**Система автоматически:**
- Проверяет валидность параметров
- Показывает понятные сообщения об ошибках
- Использует значения по умолчанию при необходимости

## Обратная совместимость

### ✅ Что продолжает работать

- Старые импорты: `from qdrant_manager import QdrantConfigManager`
- Старые скрипты: `check_status.py`, `check_and_fix.py`
- Константы в коде: `QDRANT_URL`, `API_KEY`
- Все существующие методы и функции

### 🔄 Что изменилось

- Добавлены новые файлы: `qdrant_config.py`, `qdrant_manager_with_config.py`
- Добавлена поддержка конфигурационных файлов
- Добавлена поддержка переменных окружения
- Добавлены новые методы и улучшения

### 📋 План миграции

1. **Начальный этап**: Используйте старую систему, создайте конфигурационный файл
2. **Тестирование**: Запустите тесты, убедитесь что все работает
3. **Постепенный переход**: Начните использовать новые методы в новых скриптах
4. **Полный переход**: Перенесите старые скрипты на новую систему (по желанию)

## 🎯 Рекомендации

### Для новых проектов
- Используйте новую систему с самого начала
- Храните API ключи в переменных окружения
- Создайте отдельные конфигурационные файлы для разных окружений

### Для существующих проектов
- Не спешите переписывать весь код
- Начните с создания конфигурационного файла
- Постепенно переносите новые функции на новую систему
- Используйте обе системы параллельно при необходимости

### Для production
- Используйте переменные окружения для API ключей
- Создайте отдельные конфигурации для staging и production
- Настройте систему мониторинга для отслеживания изменений

## 📞 Поддержка

Если у вас возникли вопросы:
- Проверьте [README_qdrant.md](README_qdrant.md)
- Запустите [test_config_system.py](test_config_system.py)
- Обратитесь к [USAGE.md](USAGE.md) для инструкций

---

**Важно:** Миграция не обязательна! Старая система продолжает работать, и вы можете перейти на новую систему в удобном для вас темпе.